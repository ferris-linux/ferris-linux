#!/bin/sh

. "$2"

url="https://ftp.gnu.org/gnu/libidn/libidn2-2.3.6.tar.gz"

configure() {
    ./configure --prefix="/usr" --disable-static
}

build() {
    make -j$(nproc)
}

install() {
    make install DESTDIR="$1"
}

export CFLAGS="-O2"
export CXXFLAGS="$CFLAGS"

start_installation "$1" "$url"
